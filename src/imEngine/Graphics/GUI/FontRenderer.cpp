#include "FontRenderer.h"
//#include "FontRenderer.glsl"

namespace imEngine {

/*
FontRenderer::FontRenderer(const Font &font, Window* win) {
        setFont(font);
        setWindow(win);
        setColor(Vec3(1,1,1));

        createGpuProgram();
}

void FontRenderer::setFont(const Font &font) {
        m_font = font;
}

void FontRenderer::setWindow(Window *win) {
        m_win = win;
}

void FontRenderer::setColor(const Vec3 &color) {
        m_color = color;
}

void FontRenderer::createGpuProgram() {
        m_program = ProgramPtr(new Program());
        m_program->loadSource(fontProgramSource);
        m_program->build();
}
*/


} //namespace imEngine
